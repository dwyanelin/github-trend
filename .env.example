# GitHub API token (optional locally; in GitHub Actions the built-in GITHUB_TOKEN is used)
GITHUB_TOKEN=

# LINE Messaging API — long-lived channel access token
LINE_CHANNEL_ACCESS_TOKEN=

# Alert thresholds (optional, defaults shown)
STAR_THRESHOLD_24H=1000
STAR_THRESHOLD_72H=3000
